# Database credentials (fallback)
db_user=postgres
db_password=postgres
db_host=localhost
db_name=dvdrental

# AWS Secrets Manager (primary)
DB_SECRET_ARN=arn:aws:secretsmanager:us-east-1:206409480438:secret:RdsStackPostgresDatabaseSec-o56VwWCi2M8G-j3wR36

OPENAI_API_KEY=${OPENAI_API_KEY}
LANGCHAIN_TRACING_V2=${LANGCHAIN_TRACING_V2}
LANGCHAIN_PROJECT=${LANGCHAIN_PROJECT}
LANGCHAIN_API_KEY=${LANGCHAIN_API_KEY}

# AWS credentials will be loaded automatically from:
# 1. ~/.aws/credentials file
# 2. Environment variables
# 3. IAM roles (if running on AWS)
AWS_REGION=us-east-1
